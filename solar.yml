name: solar
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.6
  - pip
  - pip:
    - numpy
    - matplotlib
    - scipy
    - astropy
    - wget
    - argparse
    - PyQt5
    - sunpy
prefix: /usr/local/anaconda3/envs/solar

